import "../css/SocialLinks.css";

const SocialLinks = ({ children }) => {
    return (
        <>
            <div className="social-icons">{children}</div>
        </>
    );
};

export default SocialLinks;
